# Almacén de datos de jugador — Upstash Redis (integración de Vercel).
# En Vercel se inyectan solas; en local, cópialas aquí o usa `vercel env pull .env`.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Puerto del servidor local (opcional; por defecto 5180).
# PORT=5180
